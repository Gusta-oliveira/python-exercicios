# Entrada de dados
metros = float(input('Informe o tamanho em metros: '))
# Processamento dos dados (conta)
c = metros * 100
m = c * 10
# Saída dos dados ao usuário
print('{} metros tem: \n {} centimetros\n{} milimetros'.format(metros, c, m))
